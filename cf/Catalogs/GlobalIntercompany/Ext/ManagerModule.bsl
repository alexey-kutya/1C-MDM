Процедура УстановитьСвязьМеждуEntityИIntercompany(Элемент, КорЭлемент, ИмяКорЭлемента) Экспорт 

	Реквизиты = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Элемент, ИмяКорЭлемента);
	Если НЕ Реквизиты[ИмяКорЭлемента] = КорЭлемент Тогда
		ЭлементОбъект = Элемент.ПолучитьОбъект();
		ЭлементОбъект[ИмяКорЭлемента] = КорЭлемент;
		Попытка
			ЭлементОбъект.Записать();
		Исключение
			
		КонецПопытки; 
	КонецЕсли; 

КонецПроцедуры
 