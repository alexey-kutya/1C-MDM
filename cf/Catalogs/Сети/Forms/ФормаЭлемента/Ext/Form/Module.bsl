
&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	Если ЗначениеЗаполнено(ТекущийОбъект.Родитель) 
		И НЕ ТекущийОбъект.ТипСети = ТекущийОбъект.Родитель.ТипСети Тогда
		
		ТекущийОбъект.ТипСети = ТекущийОбъект.Родитель.ТипСети;
		
	КонецЕсли;
	
КонецПроцедуры
