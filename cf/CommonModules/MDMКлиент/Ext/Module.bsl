Процедура ПерейтиПоСсылкеСЭД(СсылкаСЭД) Экспорт
	
	СсылкаСЭД = СокрЛП(СсылкаСЭД);
	Если ЗначениеЗаполнено(СсылкаСЭД) Тогда
		СтрокаURL = "https://docs.rusalco.com/_layouts/WSS/WSSC.V4.DMS.RLK.EDMS/GetDocByRegNum.aspx?rn="+СсылкаСЭД;
		ЗапуститьПриложение(MDMСервер.КодироватьСтрокуВUTF8(СтрокаURL));
	КонецЕсли;
	
КонецПроцедуры
